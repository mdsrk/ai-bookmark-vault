{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Deploy Frontend",
			"type": "shell",
			"command": "npx wrangler pages deploy . --project-name=ai-bookmark-vault --branch=main",
			"group": "deploy"
		}
	]
}